source "https://rubygems.org"

# Mirrors the gems GitHub Pages runs server-side, so local previews match production.
gem "github-pages", group: :jekyll_plugins
gem "jekyll-feed"
gem "jekyll-seo-tag"

# Lock to platforms GitHub Pages supports
gem "webrick", "~> 1.8"
